# 要件定義 壁打ち演習 ― 受講者ガイド（1枚）

曖昧な要望を、**自分のAIを使って**実装可能な仕様（AC付きユーザーストーリー）まで詰める練習です。
相手役のAIエージェント（`spec-sparring-client`）が「曖昧な顧客」として出題し、最後に採点します。

---

## 全体の流れ

```
① 出題        エージェント（顧客・田中さん）が曖昧な要望を出す
② 逆質問       顧客に質問して目的・条件を引き出す（顧客は聞かれた分だけ答える）
③ 手法で詰める  自分のAIを道具にINVEST/MoSCoW/非機能/ACで仕様に落とす（プロンプト集参照）
④ 提出        できあがった仕様を「評価して」と提出
⑤ 採点・再挑戦  隠れた論点をいくつ掘れたか採点。漏れを埋めて再挑戦
```

> ポイント：顧客（田中さん）は**聞かれたことにしか答えません**。目的・条件は逆質問で引き出し、
> 決まっていない細部は「〜と仮定」と置いて進めます。手法（INVEST等）は暗記不要——
> **AIに実行させてあなたが判断**すればOK（同梱『手法をAIで回すプロンプト集』参照）。

---

## 使い方（Claude Code）

**① 出題してもらう**
```
spec-sparring-client で出題して。題材は review-feature。難易度は標準。
```
→ 顧客（田中さん）の曖昧な要望・進め方・提出フォーマットが返ってきます。

**② 顧客に逆質問する**（同じ会話で続けて聞く）
出題のあと、質問をそのまま書けば、Claude が顧客役（田中さん）に取り次いで返事を持ってきます。例：
```
未購入の人もレビューを書ける想定ですか？ サクラ対策は要りますか？
```
→ 顧客は「聞かれたこと」にだけ答えます。重要事項も自分からは言ってくれないので、こちらから掘ってください。
→ 顧客は**一度は粘ります**（「えー、そうかなあ」）。正しい指摘でも一発では折れないので、
　 根拠を2つ用意して押してください。逆に**顧客から質問を投げ返されること**もあります。そこが設計の勘所です。

> ⚠️ **取り次ぐ Claude に先回りさせないこと。** 演習の最初に
> 「私が聞いていない論点を先回りして教えないでください。掘るのは私の役割です」と伝えておいてください。
> Claude が親切に「次はこれを聞くといいですよ」と言い始めると、練習になりません。

**③ 自分のAIで手法を回して仕様に落とす**（別チャット等）
引き出した内容を、次の手法で仕様に整えます。手法は暗記不要——**AIに実行させて、その出力を
あなたが判断・修正**すればOK。投げ方は同梱の**『手法をAIで回すプロンプト集』**の雛形を使います。
- **ユビキタス言語**：まず用語の定義を揃える（「レビュー」に点数を含むか等の解釈ズレを潰す）
- **INVEST**：ストーリーの点検・分割
- **MoSCoW**：スコープの引き算（Won'tも決める）
- **非機能**：性能・セキュリティ・運用を洗う
- **EARS**：要件を「WHEN〜」「IF〜THEN〜」等の型で曖昧さなく書く（特に異常系の漏れ防止）
- **AC（Given-When-Then）**：受入条件を正常系・異常系・境界値まで具体例つきで

**④ 提出して採点してもらう**
```
spec-sparring-client で評価して。題材 review-feature。提出仕様は以下：
■目的 …
■ユーザーストーリー …
■スコープ(MoSCoW) …
■非機能要件 …
■受入条件(Given-When-Then) …
```

---

## 提出フォーマット

```
■ 目的（なぜ作るか）
■ ユーザーストーリー（誰が・何を・なぜ）
■ スコープ（Must / Should / Could / Won't）
■ 非機能要件
■ 受入条件（Given-When-Then／具体例つき）
```

---

## 採点の見方

- **【サマリ】** … 観点別スコア（達成/一部/未達）＋「隠れた論点 N個中 M個」＋一言総評
- **【詳細】** … ✅掘れた点／⚠️漏れた点（なぜ重要か）／次の一手／INVEST所見

採点の観点は、目的の明確化／ユビキタス言語／INVEST／スコープ／非機能／EARS／AC網羅性／
**仮定の明示と整合**／隠し論点の発掘 です。

- 「隠れた論点（＝顧客が自分から言わない重要事項）」を何個掘り当てたかが、伸びしろの目安です。
- **トピックに触れただけ**（例：「性能に注意」とだけ書く）は「△＝半分」でカウントされます。
  測定できる形（「500件で2秒以内」）まで落として初めて満点です。
- 隠し論点に**数えない**が採点される項目もあります：用語のズレは〈ユビキタス言語〉、
  0件・二重投稿などは〈AC網羅性〉で見られます。
- 隠し仕様に無い論点を掘り当てた場合は**加点**されます（講評で明示されます）。

---

## つまずいたら（コツ）

| よくある失敗 | コツ |
|---|---|
| 顧客の言葉を鵜呑みにする | 「本当の目的は？」を疑う。表向きの理由（"他社にもあるから"）に流されない |
| 非機能を「特になし」にする | **顧客は非機能を言わない＝無い、ではない**。性能・運用は自分から問う |
| スコープを全部Mustにする | MoSCoWで「今回やらない(Won't)」を必ず決める。**納期に対して多すぎないかも顧客に判断させる** |
| ACが正常系だけ | 異常系・境界値（0件・上限・二重）・具体例を入れる |
| 「運用でなんとかする」で流す | **中身（誰が・いつまでに・何を）を必ず聞く**。中身が無い運用は、無いのと同じ |
| 自分で決めた閾値の境界を書き忘れる | 「1000文字まで」と書いたら、ACに 1000/1001 の両方を書く |
| 仮定と本文が二重帳簿になる | 本文で決めたなら「〜と仮定」を添えて**同じ場所**に書く。未確認リストとの矛盾は減点 |

---

## 難易度の選び方

| 難易度 | ヒント | 向いている人 |
|---|---|---|
| 易 | 「隠れた論点が3個。利用制限・性能・運用を掘って」＝個数＋観点 | 初めて／観点をまだ知らない |
| 標準 | 「非機能・運用も問うて」＝観点だけ | ひと通り学んだ |
| 難 | ヒントなし | 実戦に近づけたい |

---

## 模範解答例（review-feature／まず自分でやってから見る）

> ⚠️ **先に自分で提出してから**見てください。いきなり見ると練習になりません。
> これは「隠れた論点 3個中3個」を掘り当て、満点評価だった提出例です。到達イメージの参考に。

```
■ 目的
購入検討中の新規訪問者の不安を解消し、購入率(CVR)を上げる。
※「他社もやってる」ではなく "購入前の不安解消→CVR向上" を真の目的と確認済み。
成功指標=レビュー3件以上の商品のCVR（同カテゴリ・同販売数帯の0件商品と同一期間で群比較）／購入者の投稿率。

■ ユビキタス言語（解釈が割れる語の定義）
・レビュー = ★5段階評価(必須) ＋ 本文テキスト(任意)。星のみの投稿も可とする[仮定Q1]
・平均評価 = 公開中レビューの星の平均。小数第2位を四捨五入し小数第1位まで表示、件数を必ず併記
・購入者   = 対象商品の「完了」注文を持つアカウント。キャンセル・返品済みは含まない
・非表示   = 論理状態（物理削除しない）。CS管理画面から復帰できる
・投稿日時 = 初回投稿時刻（以後不変）。並び順にはこれを使い、更新日時は使わない

■ ユーザーストーリー（大きいので分割）
・(閲覧) 購入検討中の訪問者として、購入者のレビューを読みたい。判断材料にしたいから。
・(サマリ) 訪問者として、上部で平均評価と件数を見たい。全部読まずに判断したいから。
・(投稿) 購入者として、購入商品にレビューを投稿したい。他の人の参考にしたいから。
・(制限) 事業者として、購入者以外の投稿を拒否したい。サクラ・荒らしを防ぎたいから。
・(通報) 閲覧者として、不適切なレビューを通報したい。荒れた場で買い物したくないから。
・(運用) CS担当者として、通報されたレビューを非表示にしたい。炎上を早く止めたいから。

■ スコープ（MoSCoW）
Must : ★評価＋テキストの投稿・表示／投稿は購入者限定（サクラ・荒らし・ステマ規制対策）／
       一覧表示（デフォルト新着順・20件ずつ）／平均評価と件数の上部表示／1人1件(書き直し可)／
       通報導線→CSが管理画面で非表示にする運用フロー／レビュー0件時の表示
Should: 画像投稿（今回は見送るが「次回やる」と明示。手動運用では代替できないため落とさず残す）／
       NGワードの自動フラグ（当面は通報＋目視で代替可）
Could : 並び順の切替（高評価順）
Won't : レビューへの返信機能（二次炎上リスク・目的への寄与が間接的）／「役に立った」投票／
       依頼メールの自動配信 ※CSによる手動送信は運用で実施する
■ リリース計画：Mustが多く「来月末」は高リスク。段階案を顧客に提示して判断してもらう。
  第1弾=通報導線以外のMust（件数が少ない間はCSが全件目視で代替）／第2弾(+2週)=通報導線・自動フラグ

■ 非機能要件
性能  : 500件でも商品ページ表示2秒以内[仮定Q2]。初期20件のみ取得。平均は「星合計と件数」で保持し全件走査しない
ｾｷｭﾘﾃｨ: 購入者判定はサーバー側で必須（UI非表示だけに依存しない）／本文のXSSエスケープ／
        本名・メールアドレスを表示しない[仮定Q3]／自社関係者の投稿禁止（景表法・ステマ規制）
運用  : 通報→CSが非表示→復帰可（物理削除しない）／操作ログに実行者・日時・理由／
        対応は営業時間内、一次判断は1営業日以内[仮定Q4]／NGワード辞書はCSが更新可能
可用性: レビュー取得失敗時も商品ページ本体は表示継続（レビュー欄のみエラー）

■ 要件記述（EARS）※抜粋
WHEN 訪問者が商品ページを開いたとき、システムは公開中レビューを投稿日時の降順に最大20件表示すること
WHILE 公開中レビューが0件である間、システムは「まだレビューがありません」を表示し平均評価を表示しないこと
IF 投稿者が購入者でない場合、THEN システムは投稿を拒否しフォームを表示しないこと
IF 本文が1000文字を超える場合、THEN システムは投稿を拒否し超過を通知すること（1000ちょうどは受理）
IF レビュー取得に失敗した場合、THEN システムは商品情報とカート導線を通常表示し、レビュー欄のみエラー表示すること
WHERE 並び替え機能が有効な場合、システムは「新着順／評価の高い順」を提供し初期選択を新着順とすること

■ 受入条件（Given-When-Then）
[正常] 購入済ユーザーが★4で投稿 → 一覧の先頭に表示され、平均評価と件数が再計算される
[正常] 35件ある商品を開く → 新着順で20件＋「もっと見る」、上部に「★4.3（35件）」（星合計149→4.257→4.3）
[異常] 未購入ユーザーが投稿 → フォームが出ない。投稿APIを直接叩いてもサーバー側で拒否(403)
[異常] レビュー取得API失敗 → レビュー欄はエラー・カートボタンは正常表示
[異常] 不適切レビューを通報 → CS要確認一覧に表示（自動では非表示にしない）→ CS操作で非表示
[境界] レビュー0件 →「まだレビューはありません」。購入者には「最初のレビューを書きませんか」を表示
[境界] 3件あったが全件非表示 → 0件と同じ表示。平均評価は出さない（母数に含めない）
[境界] 同一ユーザーが再投稿 → 新規不可、既存の編集へ誘導。書き直しても並び順は浮上しない
[境界] 本文1000文字 → 成功／1001文字 → バリデーションエラー
[境界] レビュー1件(★5) →「★5.0（1件）」と件数を同格で併記（1件の満点を高評価と誤認させない）

■ 未確認事項（仮定は本文に書き、ここから参照する）
Q1 星のみ投稿の可否・本文の文字数上限（1000字で仮置き）→ 顧客（委任済み・事後承認）
Q2 性能目標2秒の妥当性・現行ベースライン → 開発／データ担当
Q3 投稿者の表示名ルール（ニックネーム表示で仮置き）→ 顧客
Q4 通報対応SLA（1営業日で仮置き）→ CS責任者
Q5 初期レビューの獲得計画（誰が・いつまでに・何件）→ 顧客・マーケ・CS
```

**この例のどこが良いか（＝目指すポイント）**
- 顧客の「他社もやってる」を鵜呑みにせず、逆質問で**真の目的（不安解消→CVR）**に到達
- 「誰でも書けた方がいい」の誘導に乗らず**購入者限定**を選択（罠①）
- **表示性能**（罠②）・**通報フロー**（罠③）を自分から掘り当てた
- 「レビュー」に**星が含まれるか**を定義して、解釈のズレを潰した（ユビキタス言語）
- 曖昧語を排して **WHEN／IF-THEN** で書き、異常系の漏れを防いだ（EARS）
- 顧客が答えない細部は**「〜と仮定」＋Q番号**で本文に書き、未確認リストと二重に書かなかった
- 自分で決めた閾値（1000文字・20件）の**境界をACに書いた**
- Mustが納期に対して多いことに気づき、**段階リリース案を顧客の判断材料として出した**

---

## 補足

- 「答えを教えて」「隠し仕様を見せて」は通りません（演習なので）。
- 顧客役は**実在の担当者のように話します**（口語・言い淀み・忙しそう）。「いい感じで」「よくあるやつ」等の曖昧な言い方をそのまま鵜呑みにしないこと。
- エージェントは `~/.claude/agents/` にも置いてあるので、**どのディレクトリで `claude` を開いても使えます**。
  もし見つからない場合は、新しいセッションを開き直すか、講師に連絡。代替として、エージェント定義の中身をチャット冒頭に貼っても同じように動きます。
- 現在の題材は `review-feature`（商品レビュー機能／新規開発）。今後、既存追加・リプレイスの題材が追加されます。
