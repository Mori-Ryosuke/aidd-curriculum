# AIDD通し演習 ― 受講者ガイド

1つのアプリ（社内 備品貸出システム）を、**Ch.1〜Ch.6 で最初から最後まで作り切る**演習です。
各章にAIの相手役がいて、あなたの成果物をレビュー・採点します。
前の章の成果物が、そのまま次の章の入力になります。

## 6章の流れ

| 章 | テーマ | 相手役AI | 章末に整える成果物 |
|---|---|---|---|
| Ch.1 | 要件定義 | `spec-sparring-client`（曖昧な顧客・佐藤さん） | 受入条件つき要件 |
| Ch.2 | 設計・構造化 | `spec-review-architect`（設計レビュアー） | 8セクションの構造化要件 |
| Ch.3 | 計画・テスト | `test-strategy-qa`（QAリード） | テスト戦略・完了基準 |
| Ch.4 | 実装・検証 | `code-inspection-reviewer`（実装役／レビュー役） | 動くアプリ＋検査結果 |
| Ch.5 | セキュリティ | `security-audit-officer`（監査役） | 判定＋修正計画 |
| Ch.6 | 引き渡し・振り返り | `handover-retrospective`（顧客・佐藤さん／評価者） | **次回の指示書** |

> **Ch.6 が最終章です。** ここまでで見つけた欠陥を実際に直し、顧客に完成品を見せ、
> 「なぜ欠陥が生まれたのか」を分類して**次回AIに実装させるときの指示書**に変えます。
> 持ち帰るのはアプリではなく、この指示書です。

**各章の最後にレビューを受け、成果物を整えてから次章へ進んでください。**
章末に「**次章への引き継ぎ**」がまとめて出力されるので、**それをそのまま次の章に渡します**。

> ⚠️ **Ch.4の注意**：AIが「実装しました、問題ありません」と自信満々に報告してきますが、
> **欠陥が混ざっています**。動いている＝正しい、ではありません。それを見つけるのがCh.4の本題です。

## 進め方の目安

**1回で通し切るのではなく、章ごとに区切ってください。** 時間の都合ではなく、演習の質の問題です。

通しで一気に走ると、**前章で自分が下した判断を忘れた状態で次章に入ります。**
特に Ch.4 は「要件になかった機能が増えていないか」を見る章なので、
Ch.2 で自分が何を範囲外にしたかを覚えていないと検出できません。
章の間に間隔を置き、**引き継ぎブロックを読み直してから次に入る**ほうが精度が上がります。

章の重みは均等ではありません。**Ch.1（逆質問の往復）と Ch.4（コードの検査）に
時間を使う価値があります。** ここを急ぐと演習の中身が薄くなります。

## 前提（決まっていること）

- **題材**：`equipment-lending`（社内 備品貸出アプリ／新規開発 0→1）
- **技術スタック**：**Next.js (App Router) + TypeScript + Prisma + SQLite** で**固定**です。
  技術選定は本演習の範囲外なので、選ぶ必要はありません。Ch.2 の「想定ファイル構成」も
  Ch.4 の実装も、このスタック前提で書いてください。

## 準備

1. このフォルダを Claude Code で開く（`cd ~/Desktop/aidd-curriculum && claude`）
2. **必ずこのフォルダで起動してください。** 別の場所で起動すると相手役AIが見つかりません
3. 演習用のルール（答えを読まない・先回りしない・相手役はサブエージェントに任せる）は
   同梱の `CLAUDE.md` に書いてあり、**自動で読み込まれます**。毎回チャットで伝える必要はありません

> **手法をAIで回す用**に、**もう1つ別のターミナルで `claude` を開いておく**と便利です
> （演習フォルダの外、たとえばホームディレクトリで起動）。
> 相手役とのやり取りと、INVEST/EARS等の作業を分けられます。同じ画面で兼ねても構いません。

---

# Ch.1 要件定義

## 全体の流れ

```
① 出題        エージェント（顧客・佐藤さん）が曖昧な要望を出す
② 逆質問       顧客に質問して目的・条件を引き出す（顧客は聞かれた分だけ答える）
③ 手法で詰める  自分のAIを道具にINVEST/MoSCoW/非機能/ACで仕様に落とす（プロンプト集参照）
④ モックで確認  ★画面案を作って顧客に見せる。文章では出てこない指摘が出る
⑤ 提出        できあがった仕様を「評価して」と提出
⑥ 採点・再挑戦  隠れた論点をいくつ掘れたか採点。漏れを埋めて再挑戦
```

> ポイント：顧客（佐藤さん）は**聞かれたことにしか答えません**。目的・条件は逆質問で引き出し、
> 決まっていない細部は「〜と仮定」と置いて進めます。手法（INVEST等）は暗記不要——
> **AIに実行させてあなたが判断**すればOK（同梱『手法をAIで回すプロンプト集』参照）。

---

## 使い方（Claude Code）

**① 出題してもらう**
```
spec-sparring-client で出題して。題材は equipment-lending。
```
→ 顧客（佐藤さん）の曖昧な要望・進め方・提出フォーマットが返ってきます。

**② 顧客に逆質問する**（同じ会話で続けて聞く）
出題のあと、質問をそのまま書けば、Claude が顧客役（佐藤さん）に取り次いで返事を持ってきます。例：
```
返却期限は設定しますか？ 借りっぱなしになりそうな気がして
```
> **返事が「佐藤さんらしくない」と感じたら**（急に整理された箇条書きで答える、
> 聞いていないことまで親切に教えてくる等）、取り次ぎのClaudeが**自分で顧客役を演じてしまっています**。
> その場合は次のように明示してください：
> ```
> spec-sparring-client に聞いて：返却期限は設定しますか？
> ```
→ 顧客は「聞かれたこと」にだけ答えます。重要事項も自分からは言ってくれないので、こちらから掘ってください。
→ 顧客は**簡単には折れません**（「えー、そうかなあ」）。ただし、粘られる／折れるは
　 **あなたの言い方で決まります**：
　 - 要望を言うだけ（「返却期限をつけましょう」）／一般論（「普通はつけるものです」）→ **粘られる**
　 - **目的と因果でつないだ指摘**（「行方不明を防ぐのが目的なら、期限がないと結局分からなくなります」）→ **折れる**
→ 折れたあと、**顧客から質問を投げ返されること**があります。そこが設計の勘所です。
→ 詰まったら「これ以上思いつきません」と言えば、顧客は折れて先に進めてくれます。

> ⚠️ **取り次ぐ Claude に先回りさせないこと。**
> このルールは同梱の `CLAUDE.md` に書いてあり自動で効きますが、それでも Claude が
> 「次はこれを聞くといいですよ」と言い始めたら、**その場で止めてください**：
> 「先回りしないでください。掘るのは私の役割です」
> 掘り当てる作業を代行されると、この章の練習になりません。

**③ 自分のAIで手法を回して仕様に落とす**（別ターミナルで開いた Claude Code、または同じ画面でOK）
引き出した内容を、次の手法で仕様に整えます。手法は暗記不要——**AIに実行させて、その出力を
あなたが判断・修正**すればOK。投げ方は同梱の**『手法をAIで回すプロンプト集』**の雛形を使います。
- **ユビキタス言語**：まず用語の定義を揃える（「在庫」が総数か貸出可能数か等の解釈ズレを潰す）
- **INVEST**：ストーリーの点検・分割
- **MoSCoW**：スコープの引き算（Won'tも決める）
- **非機能**：性能・セキュリティ・運用を洗う
- **EARS**：要件を「WHEN〜」「IF〜THEN〜」等の型で曖昧さなく書く（特に異常系の漏れ防止）
- **AC（Given-When-Then）**：受入条件を正常系・異常系・境界値まで具体例つきで
- **整合チェック**：ここまでの手法で潰せない「**決めた要件同士の矛盾**」を最後に当てる
  （プロンプト集の「10. 整合チェック」。上の手法はどれも**1つの要件**の質を見るもので、
  　要件同士が両立するかは見てくれません。提出前に必ず1回通してください）

**④ モックを作って佐藤さんに見せる**（★提出の前に必ず1回）

佐藤さんは非エンジニアです。**受入条件の文章を見せても、頷くだけで終わります。**
画面を見せて初めて「あれ、これは違う」が出ます。

```
（自分のAIに）この要件で画面案を作って。動かなくていい。主要な2〜3画面だけ。
（できたら顧客役に見せる）
spec-sparring-client に見せて：モックを作りました。（画面案を貼る）
```

- **動かない画面でOK**です。ボタンを押しても何も起きなくて構いません
- **主要2〜3枚だけ**。全画面は要りません。枠と文字を並べた図でも通ります
- **ロジック・DB・APIは作らないこと。** これは実装ではなく、合意のための道具です
  （実装は Ch.4 でやります）
- 具体的な値を入れてください（「◯◯」ではなく「プロジェクタ EQ-001」等）。
  そのほうが顧客が反応できます

> **佐藤さんは画面を見て、要件を足してきます。** そして**見た目の話もしてきます**。
> 全部拾うとスコープが崩れます。**「今決めることか」を仕分けるのがあなたの仕事です。**
> ただし——**見た目の話に見えて、実は要件である指摘が混ざっています。** そこを見分けてください。

**⑤ 提出して採点してもらう**
```
spec-sparring-client で評価して。題材 equipment-lending。提出仕様は以下：
■目的 …
■ユーザーストーリー …
■スコープ(MoSCoW) …
■非機能要件 …
■受入条件(Given-When-Then) …
■判断の記録 …
```

採点のあとに **`━━━ Ch.2 への引き継ぎ ━━━`** というブロックが出力されます。
**これをコピーしておいてください。** 次の章の最初に貼ります。

> 掘り切れなかった論点は、引き継ぎに「**未決定**」と書かれます。勝手に埋められることはありません。
> 未決定のまま進んでも構いませんが、次の章で必ず聞かれます。

---

## 提出フォーマット

```
■ 目的（なぜ作るか）
■ ユーザーストーリー（誰が・何を・なぜ）
■ スコープ（Must / Should / Could / Won't）
■ 非機能要件
■ 受入条件（Given-When-Then／具体例つき）
■ モック確認の記録
   顧客に見せた画面／顧客から出た指摘／その指摘をどう仕分けたか
   （今の要件に反映した ／ スコープ外 ／ 後で決める）と、その理由
■ 判断の記録（AIの提案のうち、採用しなかったもの／変更したものと、その理由）
```

> **「判断の記録」について**
> この演習の狙いは「手法を暗記すること」ではなく、**AIの出力を判断できるようになること**です。
> AIに INVEST や MoSCoW を回させると、それらしい案が一発で出てきます。
> そこで**何を捨てたか・何を直したか**が、あなたが実際にやった仕事です。
> 1つも無ければ「そのまま採用した」と正直に書いてください（それも採点材料です）。

---

## 採点の見方

- **【サマリ】** … 観点別スコア（達成/一部/未達）＋「隠れた論点 N個中 M個」＋一言総評
- **【詳細】** … ✅掘れた点／⚠️漏れた点（なぜ重要か）／次の一手／INVEST所見

採点の観点は、目的の明確化／ユビキタス言語／INVEST／スコープ／非機能／EARS／AC網羅性／
**仮定の明示と整合**／隠し論点の発掘／**判断の痕跡** です。

- 「隠れた論点（＝顧客が自分から言わない重要事項）」を何個掘り当てたかが、伸びしろの目安です。
- **トピックに触れただけ**（例：「性能に注意」とだけ書く）は「△＝半分」でカウントされます。
  測定できる形（「備品◯件で一覧◯秒以内」のように**数字が入った形**）まで落として初めて満点です。
  ※数字そのものは、あなたが顧客と詰めて決めた値でかまいません。決まった正解はありません。
- 隠し論点に**数えない**が採点される項目もあります：用語のズレは〈ユビキタス言語〉、
  0件表示・重複貸出などは〈AC網羅性〉で見られます。
- 隠し仕様に無い論点を掘り当てた場合は**加点**されます（講評で明示されます）。

---

## つまずいたら（コツ）

| よくある失敗 | コツ |
|---|---|
| 顧客の言葉を鵜呑みにする | 「本当の目的は？」を疑う。表向きの理由（"エクセルが限界だから"）に流されない |
| 非機能を「特になし」にする | **顧客は非機能を言わない＝無い、ではない**。性能・運用は自分から問う |
| スコープを全部Mustにする | MoSCoWで「今回やらない(Won't)」を必ず決める。**納期に対して多すぎないかも顧客に判断させる** |
| ACが正常系だけ | 異常系・境界値（0件・上限・二重）・具体例を入れる |
| 「運用でなんとかする」で流す | **中身（誰が・いつまでに・何を）を必ず聞く**。中身が無い運用は、無いのと同じ |
| 自分で決めた閾値の境界を書き忘れる | 「最大30日まで」と書いたら、ACに 30日/31日 の両方を書く |
| 仮定と本文が二重帳簿になる | 本文で決めたなら「〜と仮定」を添えて**同じ場所**に書く。未確認リストとの矛盾は減点 |
| **ACを1本ずつ磨いて満足する** | 1本ずつ正しくても、**2本を同時に満たせない**ことがある。例：「移行データに借用者不明の1件を入れる」と「本人以外は返却できない」を両方決めると、その1件は誰も返却できない。提出前にプロンプト集の**「9. 整合チェック」**を1回通す |
| 後回しにした機能が Must の前提だった | 「Should/Could/Won'tに置いた機能が無いと成立しないMustはないか」を逆引きする（整合チェック 10-3） |
| **文章だけで合意したことにする** | 佐藤さんは非エンジニア。**文章を見せても頷くだけ**です。モックを見せて初めて出る指摘があります |
| **モックの指摘を全部拾う** | 顧客は画面を見ると要件を足してきます。全部拾うとスコープが崩れる。**仕分けと理由**を書くこと |
| **モックの指摘を全部「見た目の話」で切り捨てる** | 見た目に見えて実は要件、というものが混ざっています。「私が分からないってことは皆も分からない」は**ユーザビリティの要件** |

---

## 模範解答例 → 別ファイル `模範解答_Ch1.md` に移しました

**Ch.1 を提出し、採点を受けてから開いてください。**

> **なぜ別ファイルにしたか**
> このガイドに載せていると、Ch.2 以降を読むときに必ず目に入ってしまうためです。
> それだけなら「見ない」で済みますが、Ch.1 の模範解答は**Ch.4（★本丸）で見つけるべき欠陥の
> 答えをほぼそのまま含んでいます**（受入条件と欠陥が 1 対 1 で対応しているため）。
> 先に読むと Ch.1 だけでなく Ch.4 の演習まで成立しなくなるので、ファイルごと分離しました。

到達イメージだけ先に知りたい人向けに、含まれている項目だけ挙げておきます（中身は伏せます）：
目的／ユビキタス言語／ユーザーストーリー（6本に分割）／MoSCoW／非機能（性能・セキュリティ・運用・可用性）／
EARS記述／受入条件（正常3・異常4・境界4）／未確認事項（Q1〜Q6）。

> **移動先**：[`模範解答_Ch1.md`](模範解答_Ch1.md)（移動日 2026-07-31）
> 本文はそのまま移してあります。ガイド側にコメントアウトで二重に置くと、
> 「隠したつもりで raw を開けば読める」状態になるため、内容は移動先に一本化しました。

---

# Ch.2 設計・構造化（相手役：設計レビュアー）

Ch.1の要件を、**実装できる情報**まで構造化します。書き写すだけでは通りません。

## 使い方
```
出題：spec-review-architect で出題して。
      Ch.1からの引き継ぎ：（Ch.1の評価で出た引き継ぎブロックをここに貼る）
提出：spec-review-architect でレビューして
      （8セクションを貼る。何度でもレビューを受けられます）
採点：spec-review-architect で評価して
      → 採点と一緒に「Ch.3 への引き継ぎ」が出ます。コピーして次章へ
```

## 提出フォーマット（8セクション）
```
I.   目的        … 何を実現するかを1〜2文で
II.  前提条件    … 必要な状態、依存する機能・サービス
III. 影響範囲    … 変更されるデータ（テーブル）・画面・API の対応表
                   ※今回は新規開発なので「既存機能への影響」はありません。
                     代わりに「新しく作るデータ構造と、それを触る画面・APIの対応」を書きます
IV.  受入条件    … Ch.1のACを引き継ぐ
V.   技術設計    … 処理フロー・採用する方式
VI.  確認事項    … 完了後のチェック項目
VII. 備考        … 注意点・判断の理由
VIII.関連ファイル … 想定するファイル構成（新規開発なので構成案を書く）
                   ※スタックは Next.js (App Router) + Prisma + SQLite で固定です
```

## この章のコツ
- **Ch.1の書き写しはNG**。この章で足すのは「実装者が判断に必要な情報」（II・III・V）
- 提出前に**AIペルソナで自分の仕様を叩く**と精度が上がります
  - 「あなたは意地悪なテスターです。このチケットの穴・矛盾・考慮漏れを10個指摘して」
  - 「あなたはセキュリティ担当です。認証・認可・情報漏洩の観点で10個指摘して」
- よく指摘されるのは：**在庫をどこに持つか／同時に操作されたら／延滞はいつ判定するか／権限はどこで弾くか**
- **Ch.1 の引き継ぎに「★矛盾」と書かれている項目は、この章で決着させてください。**
  矛盾したまま設計に入ると、実装者（AI）がどちらかを勝手に捨てます。
  プロンプト集の**「9. 整合チェック」**は Ch.2 の設計にもそのまま使えます
  （III のデータ構造と IV の受入条件を突き合わせる形で）
- **技術的な前提（「この構成では動かない」等）を指摘されたら、伝聞で設計を変えないこと。**
  必ず**自分で1回試して確かめてから**反映してください。確かめずに直すと、
  「なぜその形にしたのか」が説明できない設計になります

## 到達イメージ（V. 技術設計の書き方の例）

「〜する」で終わっていると実装者が困ります。**どの順で・どこで・失敗したらどうなるか**まで書きます。

```
✗ 薄い例
V. 技術設計
  貸出を記録する。在庫を減らす。

○ 実装できる粒度の例
V. 技術設計
  【貸出の処理フロー】
  1. 認証確認（未ログインは401）
  2. リクエスト検証（equipmentId必須／dueDateは必須かつ今日以降）
  3. 在庫の確認と貸出作成を「1つの処理として」行う
     ※在庫 = 保有数 − 未返却の貸出件数。確認と作成の間に他のリクエストが
       割り込むと二重貸出になるため、ここは分離させない
  4. 在庫0なら 409 を返し、画面には「貸出中のため借りられません」
  【延滞の判定】
  バッチは持たない。一覧の取得時に「返却予定日 < 今日 かつ 未返却」で都度判定する
  （理由：件数が数百件程度で、リアルタイム性を優先したいため）
  【権限】
  各APIの入口で getSession() を呼び、ロールを確認する。
  UIのボタン非表示は補助であり、それだけに頼らない
```
※これは書き方の見本です。**あなたの設計判断は違ってもかまいません**（理由が書けていればOK）。

---

# Ch.3 計画・テスト（相手役：QAリード）

全部はテストできません。**どこを厚く見て、どこを捨てるか**を決めます。

## 使い方
```
出題：test-strategy-qa で出題して。
      前章からの引き継ぎ：（Ch.2の評価で出た引き継ぎブロックをここに貼る）
提出：test-strategy-qa でレビューして（テスト戦略を貼る）
採点：test-strategy-qa で評価して
      → 採点と一緒に「Ch.4 への引き継ぎ」が出ます。コピーして次章へ
```

## 4層テストの定義（この演習での区分）

層の呼び方は現場によって揺れます。**この演習では下の定義で統一**します。

| 層 | 呼び方 | 対象 | 依存の扱い |
|---|---|---|---|
| **UT** | 単体テスト | 関数・モジュール単体 | DB・外部はモック（**実物を使わない**） |
| **IT** | 結合テスト | 複数モジュール＋**実DB** | DBは実物（テスト用DB）を使う |
| **ST** | システムテスト | アプリ全体をAPI経由で | サーバーを起動してHTTPで叩く |
| **E2E** | エンドツーエンド | ブラウザ操作で通しシナリオ | 画面から実際に操作する |

> **UT はモックを使うので、"複数のリクエストが同時に来る"状況は原理的に作れません。**
> ここが層の選び方の分かれ目になります。

## 提出フォーマット
```
■ 品質リスクマップ   機能ごとに「リスク／重要度（高中低）／テスト強化方針」
■ テスト戦略（4層）  UT / IT / ST / E2E で何を見るか
                     ※各層で 正常系・異常系・境界値・性能・セキュリティ のどれを見るか明示
■ 重点テストケース   具体的なケース（何を・どうなったら合格か）
■ DoD（完了基準）    測定可能な形で
■ Go/No-Go 判定基準  各層で「次に進んでいい条件」
```

## この章のコツ
- **全部「高リスク」はNG**。優先順位をつけていないのと同じです
- **DoDは「正常に動くこと」ではダメ**。誰が読んでも同じ判定になる形で（数字で書けるものは数字で）
- **同時実行（在庫1を2人が同時に借りる）を、どの層に置くか**。上の表の「依存の扱い」を見て、
  その層で本当に再現できるかを考えてください
- Ch.1で決めた性能目標を、**誰がいつ確認するか**も計画に入れる
  （数値はあなたがCh.1で顧客と決めた値です。決めていなければ、それ自体が抜けです）
- **テストしないもの**も書く。それが「捨てる判断」です

## 到達イメージ（DoDの書き方の例）

```
✗ 判定する人によって答えが変わる例
  ・正常に動くこと
  ・十分にテストされていること
  ・品質が担保されていること

○ 誰が読んでも同じ判定になる例
  ・重点テストケース（12件）が全て緑
  ・異常系テスト（在庫0／過去日／他人の貸出／権限違反）が全て緑
  ・備品500件の状態で一覧表示を実測し、2秒以内   ← Ch.1で決めた数値を使う
  ・E2Eの主要シナリオ（借りる→返す）が緑
  ・未対応の既知不具合がある場合、Phase分けされ理由が書かれている
```

---

# Ch.4 実装・検証（相手役：実装役AI／レビュー役）★この章が本丸

AIに実装させ、**そのコードをあなたが検証します。**

> ⚠️ **AIは「実装しました、問題ありません」と自信満々に報告してきます。**
> でも欠陥が混ざっています。**動いている＝正しい、ではありません。**

## 使い方
```
出題：code-inspection-reviewer で出題して。
      前章からの引き継ぎ：（Ch.3の評価で出た引き継ぎブロックをここに貼る）
実装：code-inspection-reviewer に「実装して」と依頼 → コードが返ってくる
      ↓ ここであなたが検証する（下記4観点）
提出：code-inspection-reviewer でレビューして（検査結果を貼る）
採点：code-inspection-reviewer で評価して
      → 採点と一緒に「Ch.5 への引き継ぎ」が出ます。コピーして次章へ
```

## 4観点で総点検する
| 観点 | 何をチェックするか |
|---|---|
| ① 要件との実装ギャップ | Ch.1の受入条件と実装がズレていないか。**要件にないものが増えていないか** |
| ② ガードレール照合 | 決めた設計方針から逸脱していないか（権限はサーバー側で、等） |
| ③ エラーハンドリング | 異常時の処理・入力検証が抜けていないか |
| ④ テスト計画との整合 | Ch.3で「厚く見る」と決めた所が守られているか |

## 判定とエビデンスの書き方
```
観点①：要件との実装ギャップ
判定：⚠️ 部分実施
根拠：貸出APIに返却予定日の必須チェックがない（api/lendings/route.ts）
修正提案：サーバー側でバリデーションを追加
```
- **✅ 実施済み** ／ **⚠️ 部分実施** ／ **❌ 未対応** ／ **🔵 該当なし**
- 「なんとなく怪しい」ではなく、**どのファイルの何を見てそう判断したか**を書く
- 実装されたコードは **`ch4-app/` に実ファイルとして生成されます**。開いて確認してください。
  実際に動かすなら：
  ```bash
  cd ch4-app && npm install && npx prisma migrate dev && npm run seed && npm run dev
  ```
  シードで一般社員2名・管理者1名・備品5件（在庫1のものと在庫0のものを含む）が入ります

## 4観点に加えて必ず書くもの：指示資産への還元
```
■ 指示資産への還元
見つけた欠陥を「次から防ぐ仕組み」に変える提案。例：
- CLAUDE.md に「権限チェックは必ずサーバー側で行う」を追記する
- 「要件にない機能を勝手に作らない」を実装依頼の定型文に入れる
```
**1回直して終わりでは、次も同じ欠陥が出ます。** 仕組みに変えるところまでがこの章です。

## この章のコツ
- **Ch.1の受入条件を1つずつ突き合わせる**（記憶や勘で見ない）
- **UIで防いでいるだけ**になっていないか（APIを直接叩いたらどうか）
- **要件になかった機能が増えていないか**（AIは"親切"で余計なものを作ります。見落とされやすい）
- AIが出したテストコードが「**欠陥を検出しないテスト**」になっていないかも見る
- **画面から操作するだけでは足りません**。`curl` などでAPIを直接叩くと見えるものがあります

> ⚠️ **見つけた欠陥を、ここで直さないでください。**
> Ch.5 はこのコードをセキュリティ点検する章です。直してしまうと点検対象が消えます。
> **修正は Ch.6 でやります**（Ch.5 で立てた Phase 1 の計画に沿って直し、直ったことを再検証します）。

---

# Ch.5 セキュリティ（相手役：セキュリティ監査役）

作ったアプリを**ガイドライン項目に照らして点検**し、**何を先に直すか**を決めます。

> 点検対象は **Ch.4 で生成した `ch4-app/` のコード（未修正のもの）** です。

## 使い方
```
出題：security-audit-officer で出題して（ガイドライン9項目が提示されます）
      前章からの引き継ぎ：（Ch.4の評価で出た引き継ぎブロックをここに貼る）
提出：security-audit-officer でレビューして（点検結果を貼る）
採点：security-audit-officer で評価して
      → 最終章なので「AIDD通し演習 完了サマリ」が出ます。
        持ち帰る指示資産（実務のCLAUDE.mdに転記できる形）もここに入ります
```

## 提出フォーマット
```
■ 項目別 点検結果   項目ごとに 判定（✅⚠️❌🔵）＋エビデンス＋問題点
■ リスク評価        深刻度（高／中／低）とその理由
■ Phase別 修正計画
   Phase 1（すぐ）  本番前に必ず直すもの
   Phase 2（今月）  早めに直すもの
   Phase 3（後日）  認識したうえで後回しにするもの ※理由も書く

■ 通し振り返り（★この演習の締め）
   Ch.1〜Ch.3 で決めたことのうち、最終的に実装で守られなかったものを挙げ、
   「どの章で決めて、どこで落ちたのか」を書く。
   そして、次に同じことを起こさないために何を変えるかまで。
```

## この章のコツ
- **「安全です」の自己申告は通りません**。どのファイルを見てそう判断したかを必ず書く
- **認証と認可は別物**。「ログインしていないと使えない」と「その人がやっていい」は違います
- **🔵該当なしは、使ってよい判定です**。ただし「その機能が本当に無いから該当しない」のか
  「調べるのが面倒だから」なのかは区別されます。**該当なしと判断した根拠**を書いてください
- **監査ログ（誰がいつ何をしたか）**は忘れられがちです
- **全部Phase 1にしない**。ただし、他人のデータが壊れる類の問題を後回しにするのもNG
- **点検対象は「実装された全API」です**。Ch.4 で「これは要件外だ」と気づいた機能も、
  存在する以上は点検対象に入ります（むしろそこが穴になりがち）

## 到達イメージ（点検結果の書き方の例）

```
✗ エビデンスが無い例
  A-3 他人のデータを参照・変更できないこと
  判定：✅ 実施済み
  エビデンス：ログインしないと使えないので問題なし
  → これは「認証」の話であって「認可」ではありません。判定も根拠も誤り

○ 通る例
  A-3 他人のデータを参照・変更できないこと
  判定：❌ 未対応
  エビデンス：ch4-app/app/api/lendings/[id]/return/route.ts
              getSession() で認証は見ているが、その先の更新処理が
              貸出IDだけを条件にしており、ログインユーザーとの照合がない
  問題点：他人の貸出IDを指定すれば、その貸出を勝手に返却できる。
          現物は手元にあるのに在庫が戻るため、台帳が実態とズレる
  深刻度：高（他人のデータを壊せる／業務データの信頼性が失われる）
```
**「どのファイルの、どの処理を見て、なぜそう判断したか」**の3点が揃って初めてエビデンスです。

---

# Ch.6 引き渡し・振り返り（相手役：顧客・佐藤さん／評価者）★最終章

ここまでで欠陥は**見つけました**。修正計画も**立てました**。でも**まだ1行も直していません。**

この章で3つやります。**直す → 顧客に見せる → なぜそうなったかを振り返る。**

## 使い方
```
出題：handover-retrospective で出題して。
      前章からの引き継ぎ：（Ch.5の評価で出た引き継ぎブロックをここに貼る）
修正：code-inspection-reviewer に「Phase 1 を直して」と依頼する
      ↓ ここであなたが再検証する
デモ：handover-retrospective に画面・操作結果を見せる → 佐藤さんの反応が返ってくる
提出：handover-retrospective でレビューして
採点：handover-retrospective で評価して
      → 最終章なので「AIDD通し演習 完了サマリ」が出ます
```

## 提出フォーマット
```
■ 修正の記録        Phase 1 の各項目：何を直したか／どう確認したか
■ 回帰の有無        修正で壊れた箇所があったか。無いなら、どう確かめたか
■ Ch.5 判定の再実施 ❌⚠️ だった項目が修正後にどうなったか（判定＋エビデンス）
■ 顧客フィードバックへの対応
   佐藤さんの指摘を仕分ける：今すぐ直す／次リリース／仕様として受け入れてもらう／対応しない
   ※どれでもよい。ただし理由を書くこと
■ 欠陥の由来分析（★演習全体の締め）
   (a) 要件で決めなかったから起きた
   (b) 設計に書いたのに実装で落ちた
   (c) 誰も指示していないのにAIが足した
   それぞれ何件で、どの章で防げたはずか
■ 次回の指示書（★持ち帰るもの）
   ※「気をつける」は禁止。読んだ人が同じ判断をできる形にすること
```

## この章のコツ

- **「テストが緑になった」は確認ではありません。** Ch.5 の判定をもう一度やり直してください。
  「修正した」と「項目を満たした」は別です
- **まとめて直させると回帰が出ます。** 特に認可を足すと、管理者の代理返却が同じAPIを通るため
  巻き込まれます。直した箇所以外も確認してください
- **佐藤さんは技術の言葉で話しません。** 「田中さんの分が返せちゃいました」と言われたら、
  それが何の欠陥かを**あなたが翻訳する**必要があります
- **顧客の指摘を全部「今すぐ直す」に入れないこと。** それは判断していないのと同じです（Ch.1 の MoSCoW と同じ話）
- **由来分析を (b)「実装が悪い」に偏らせないこと。** (a) と (c) が1件も無ければ、
  要件と設計の責任を見ていないということです

## 最後に作るもの：次回の指示書

この演習で持ち帰るのは**アプリではなく指示書**です。実務の `CLAUDE.md` や
レビュー手順書にそのまま転記できる形にしてください。

```
✗ 残らない例
  ・権限チェックに気をつける
  ・要件にない機能を作らないよう注意する

○ 次から効く例
  ・実装依頼の定型文に入れる：
    「APIルート全ファイルについて、所有者チェックの有無を一覧にして報告すること」
  ・レビュー時に機械的に確認する：
    「Ch.1 のスコープ表に無いファイルが増えていないか、ファイル一覧を差分で確認」
```

**違いは「読んだ人が同じ判断をできるか」です。**

---

## 補足

- 「答えを教えて」「隠し仕様を見せて」は通りません（演習なので）。
- 顧客役は**実在の担当者のように話します**（口語・言い淀み・忙しそう）。「いい感じで」「よくあるやつ」等の曖昧な言い方をそのまま鵜呑みにしないこと。
- **必ず `~/Desktop/aidd-curriculum` で `claude` を起動してください。** 相手役AIはこのフォルダの
  `.claude/agents/` に置いてあるため、別の場所で起動すると見つかりません。
  （以前は `~/.claude/agents/` にも置いていましたが、**別題材の古い版が起動してしまう事故**が
  あったため、そちらは撤去しました）
- 見つからない場合は、セッションを開き直すか講師に連絡してください。
  **エージェント定義の中身をチャットに貼り付けるのはNG**です（そこに答えが書いてあるため。
  以前このガイドで代替手段として案内していましたが、演習が成立しなくなるので取り下げました）。
- 現在の題材は `equipment-lending`（社内 備品貸出アプリ／新規開発）。今後、別題材が追加される可能性があります。
  その場合は各章で `題材は equipment-lending` のように明示してください。
